spring:
  application:
    name: market
  profiles:
    include: secret
  datasource:
    url: jdbc:mysql://localhost:3306/market_db?serverTimezone=Asia/Seoul
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
